import React, { Component } from 'react';

class App extends Component {
  render() {
    const text = 'React Redux Boilerplate';

    return (
      <div>
        {text}
      </div>
    );
  }
}

export default App;
